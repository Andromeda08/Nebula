#version 460
#extension GL_EXT_mesh_shader : require

layout (local_size_x = 32) in;

struct Task {
    uint wgID;
    uint8_t pps[31];
};

taskPayloadSharedEXT Task tout;

const uint baseID = gl_WorkGroupID.x * 32;

void main() {
    EmitMeshTasksEXT(32, 1, 1);
}